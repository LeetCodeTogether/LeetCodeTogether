import React from "react";

class Lobby extends React.Component {
    render = () => {
        return(
            <div className="lobby">
                <h1>This is a lobby!</h1>
            </div>
        );
    }
}

export default Lobby;